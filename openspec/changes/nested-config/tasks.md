## 1. Shared helpers

- [ ] 1.1 Re-derive the check list from nats-server v2.14.7 (`8d8b69a`, the corpus clone) `server/stream.go` `checkStreamCfg`, `server/sublist.go` `ValidateMapping` and `server/subject_transform.go`, and from nats.go v1.53.1 `convertDomain` / `prepareKeyValueConfig`; record any difference from design.md's Context in design.md before writing code; verify by diffing the same functions against the local `main` checkout (`7cbac9c`) and noting the result
- [ ] 1.2 Write `internal/natsapi/transform_test.go` with every row of the server's `TestValidateDestinationSubject`, every non-strict `shouldErr`/`shouldBeOK` row of `TestSubjectTransforms` (plus the strict-accepted `foo.*` → `bar.{{Wildcard(1)}}` row), and error-text rows for `{{split(3,1)}}` out of range, `{{wildcard(1)}}{{split(3,1)}}` too many arguments, `{{unknown(1)}}` unknown function and `""` → `archive.orders` invalid subject; verify it fails to build before `transform.go` exists
- [ ] 1.3 Port `ValidateMapping` and the non-strict error paths of `NewSubjectTransformWithStrict` (with `indexPlaceHolders`, `transformIndexIntArgsHelper`, `subjectInfo`, the ten function expressions verbatim and the server's error values) into `internal/natsapi/transform.go` as `ValidateMapping` and `SubjectTransformErr`; verify `go test ./internal/natsapi/` passes
- [ ] 1.4 Add `Fields.Lits` (struct/pointer field literal or slice element literals, `complete` flag, masked or non-literal values make it incomplete) with table tests for `&T{}`, `T{}`, `[]*T{{…}, v}`, `[]T{{…}}`, a variable, `nil` and a masked field; verify `go test ./internal/natsapi/` passes
- [ ] 1.5 Extend `AssignedFields` to record `x.F[i] = …` (and nested index chains) as assigning `F`; add the "Indexed element assignment" scenario to the masker tests; verify `go test ./internal/natsapi/` and `go test ./analyzers/...` pass unchanged otherwise

## 2. streamconfig nested literals

- [ ] 2.1 Write `testdata/streamconfig/nested.go` (jetstream) and `nested_legacy.go` with one `// want` line or silent line per scenario of the modified and added requirements in `specs/rule-streamconfig/spec.md`, messages copied from the spec, including the standalone `RePublish` variable, the KeyValue-parent cases, the legacy and `jetstream` KeyValue-source transform scenarios and the `cfg.Sources[0].FilterSubject = ""` and `cfg.Subjects[1] = "b"` masking cases; verify `go test ./analyzers/streamconfig/` fails on the new expectations only
- [ ] 2.2 Implement the context-free checks at the nested literal's own visit: transform source validity, transform mapping via `natsapi.ValidateMapping`, republish mapping via `natsapi.SubjectTransformErr`, filter plus transforms, pairwise subset overlap (and `SubjectsCollide` when the stack shows `Mirror:` under a stream or KeyValue config literal), `Domain` plus `External`, and the durable-source consumer checks read from the `StreamSource` literal, with the transform checks skipped when the stack passes through the `Sources` of a `nats.KeyValueConfig` literal; verify the analysistest passes for those scenarios
- [ ] 2.3 Implement the parent checks at the stream config visit through `Fields.Lits`: sourced/mirrored stream names (including `nil` source elements, skipping a mirror with `External` or `Domain`) and the republish cycle with the default subject and the implicit republish; verify the whole streamconfig analysistest passes
- [ ] 2.4 Update the `streamconfig` Doc string so it describes the nested checks (it is the generated rule documentation); verify `make generate` updates `docs/rules.md` and `TestRulesDocUpToDate` passes

## 3. kvconfig republish cycle

- [ ] 3.1 Add the scenarios of `specs/rule-kvconfig/spec.md` to `testdata/kvconfig` (jetstream and legacy files); verify `go test ./analyzers/kvconfig/` fails on the new expectations only
- [ ] 3.2 Implement the cycle check on KeyValue config literals (constant bucket passing `BucketValid`, no `Mirror`, `RePublish` literal via `Fields.Lits`, `SubjectsCollide` against `$KV.<bucket>.>`), and rewrite the `kvconfig` Doc string to cover one server-side check; verify the analysistest passes and `make generate` leaves `docs/rules.md` consistent

## 4. Corpus, docs and gates

- [ ] 4.1 Run `make corpus` (outside the sandbox); triage every new line into `scripts/corpus.expected` — nats-server test sites asserting the rejection as `FP` with that reason, anything else as `TP` with what is wrong and a paste-ready issue draft, or `FP` with a one-line reason — and note any line the masking change removed; verify a second `make corpus` passes
- [ ] 4.2 Run `make plugin`; verify the golangci-lint plugin output still matches `natsvet` over `testdata`
- [ ] 4.3 Update `docs/design.md` §3.1 `streamconfig` and `kvconfig` with the nested checks, strike §8 item 10, close §8 item 8 as won't-do (nats.go's `processConsInfo` fails only when `FilterSubject != "" && subj != FilterSubject`, i.e. on the bound consumer's server-side config, and `subject` already skips these calls), and record the corpus outcome in §6 (coordinate with any uncommitted `docs/design.md` edits in the working tree rather than overwriting them); verify the README rule table still matches `Analyzers()`
- [ ] 4.4 Run `gofmt -l .`, `go vet ./...`, `staticcheck ./...`, `misspell -locale US .`, `make lint` and `make test` (with the testdata download step); verify all pass with no output from gofmt and misspell
