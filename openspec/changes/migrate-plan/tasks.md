## 1. Facts and the mapping table

- [x] 1.1 Re-derive the facts in design.md's Context from nats.go v1.53.1 (`jetstream.go`, `jsm.go`, `js.go`, `kv.go`, `object.go`, `jetstream/pull.go`, `jetstream/MIGRATION.md`) and nats-server v2.14.7 (`consumer.go`); diff the ordered-consumer option sets (`nats.OrderedConsumer` vs `jetstream.OrderedConsumerConfig`) and the lister shapes (`Streams()`/`StreamNames()` channels vs `StreamInfoLister`/`StreamNameLister`); record every difference or confirmation in design.md, resolving its two open questions; verify each fact is cited to a function
- [x] 1.2 Write `internal/migrate/table_test.go`: every key of `natsapi`'s legacy table has an entry; every entry's target resolves in the pinned `jetstream` package loaded from the testdata module (skip when unavailable, as `TestTablesUpToDate` does); every two-column legacy-to-new row of the pinned `jetstream/MIGRATION.md` agrees, with a failure when zero rows are parsed; the table's recorded nats.go version equals the testdata pin; verify it fails before the table exists
- [x] 1.3 Write the mapping table in `internal/migrate/table.go` for all 324 legacy symbols — JetStream, KeyValue and object store — (kind, target, shape, decision patterns, unmapped reason, `MIGRATION.md` anchor), including `AddConsumer` → `CreateConsumer`, the stream-handle acquisitions with their `STREAM.INFO` note, `StreamNameBySubject` for unbound subscriptions, `AckSync` → `DoubleAck`, `Heartbeat` → `IdleHeartbeat`, `MaxWait` → `WithDefaultTimeout`, and the subscribe-option-to-field rows; verify `go test ./internal/migrate/` passes the table tests

## 2. Loader and inventory

- [x] 2.1 Create `testdata/migrate/` packages covering the spec scenarios: a service with a handle on a struct used from a second package, independent handles, an external boundary (a legacy handle passed to a stub module outside the loaded set), management, publish, KeyValue and object store calls with and without a context in scope and with `nats.Context`, callback, sync, channel and queue subscriptions (bound and unbound, explicit durable, named ephemeral, `ManualAck`, a push-only option), a pull subscription with a `Fetch` loop, a stream created in the module that covers an unbound subscription's subject, a handler shared with a core subscription, a legacy-only option, a test file of legacy-API tests to skip, and a package with no legacy use; a second module variant requires an older nats.go for the step-0 case; verify both build against their nats.go
- [x] 2.2 Implement the loader (`go/packages`, tests included, test variants merged by position, load and type errors fail the command, the module's nats.go requirement) and the inventory (legacy identifiers by `natsapi.LegacySymbol`, `*nats.Msg` uses in handlers passed to legacy subscribe calls, grouping into sites); verify a test that the plan's legacy identifier count equals the number of `legacyjs` diagnostics over `testdata/migrate`, and that core-only code yields no sites

## 3. Classification

- [x] 3.1 Implement the context rule (scope lookup, then `nats.Context`, then `context.Background()`) with a table-driven test over the three spec scenarios
- [x] 3.2 Implement mechanical classification and replacements by splicing original argument source into target templates, each as `before`/`after` text and byte-offset edits; verify golden replacements for the spec's scenarios (consumer creation, purge through a stream handle with its note, options folded into a config with `FilterSubject`, the runtime stream lookup for an unbound subscription) and that every emitted `after` parses with `go/parser`
- [x] 3.3 Implement guided sites (the `Fetch` loop with its facts, per-call `MaxWait`) and unmapped sites with their reasons; verify golden output for the `Fetch` loop and the legacy-only option
- [x] 3.4 Implement the decision patterns with their options, behavior-preserving defaults (pull for `subscribe-target`), reasons and per-option replacements (`subscribe-target`, `ack`, `push-only-option` only after pull, `channel-max-ack-pending`, `shared-handler`, `component`); verify golden output for the unbound callback subscription (`subscribe-target` and `ack`), the `ManualAck` variant (no `ack`), the bound pull subscription (no `subscribe-target`) and a push-only option under push (no `push-only-option`)
- [x] 3.5 Implement the durable note (explicit durables only; never a `DeleteConsumer`) and follow-ups (static stream name, with the candidate stream and its position when exactly one stream config in the loaded code covers the subject); verify the spec's durable, named-ephemeral and two follow-up scenarios

## 4. Components, steps and decisions

- [x] 4.1 Implement components (roots, union-find over objects via assignment, arguments, returns, fields, composite literals; boundaries for outside packages and for excluded tests) and the dual-handle step order with `<name>New` siblings, removal and rename, the one-commit hint, and step 0 when the module's nats.go is older than the table's; verify the component scenarios, the hint and step 0 on `testdata/migrate`
- [x] 4.2 Implement the decisions file (`natsvet-migrate.json` at the module root by default, `-decisions` override, never written by `plan`; scope resolution with narrowest match; reclassification of answered sites; `component: skip` semantics; errors for unknown patterns, options and scopes; reporting answers whose scope no longer exists); verify the spec's decision scenarios, including the skipped component and byte-identical output across two runs

## 5. Output, command and skill

- [x] 5.1 Implement the JSON plan model (`schema_version: 1`, the table's nats.go version, per-file original SHA-256, per-step expected hashes, `before`/`after` and edits in each step's coordinates, stable order, no maps) and the Markdown renderer (`text/template`, with the follow-ups section); verify golden JSON and Markdown for `testdata/migrate`, a test that every site, pending decision, step and follow-up in the JSON appears in the Markdown, and a test that edits within a step never overlap
- [x] 5.2 Write `internal/migrate/SKILL.md` (the loop; the decision protocol — ask the user each pending pattern with its default, record answers in `natsvet-migrate.json`, never pick silently, commit the file with the first step that uses it, re-plan after any manual edit; `schema_version: 1`), embed it in `internal/migrate`, and point the plan header at `natsvet migrate skill`; verify a test that the embedded skill's version equals the plan's
- [x] 5.3 Add `migrate.Main` (`plan`, `skill`) and the dispatch in `cmd/natsvet` before `multichecker.Main`; verify `natsvet migrate plan ./...` in `testdata/migrate` prints the golden plan and leaves the tree unchanged, `natsvet migrate skill` prints the embedded skill, and `natsvet ./...`, `natsvet -legacyjs.enable ./...` and `go vet -vettool` over `testdata` report exactly what they did before (compare against a build of `main`)

## 6. Proof, validation and docs

- [x] 6.1 Write the test-only applier (apply steps in order, each step's edits from the highest offset down, refusing a file whose hash differs from the step's recorded hash, stop before a component's removal and rename steps while it has guided sites) and the testdata proof: copy `testdata/migrate`, answer all decisions with defaults, apply every step that carries edits in order and type-check after each; at the end `legacyjs` reports exactly the legacy uses of the sites whose steps were not applied (guided, unmapped and undecided sites, skipped components, and the roots and declarations of components whose removal waits), and every other natsvet rule reports what it did before; also verify the applier refuses a file edited after planning; verify `go test ./internal/migrate/` passes
- [x] 6.2 Add the natscli end-to-end test, skipped unless `NATSVET_MIGRATE_CORPUS` points at the corpus cache: copy natscli, plan with default answers, apply every step that carries edits with a type-check after each, and require that `legacyjs` then reports exactly the legacy uses of the sites whose steps were not applied and that the other rules report what they did before; run it (outside the sandbox) and fix every classification bug it exposes, each with a testdata case
- [x] 6.3 Write `scripts/migrate-corpus.sh` and run it over go-choria and eventing-natss from the corpus cache (outside the sandbox); review their components against what a human would call a unit of work, fix classification bugs it exposes (each with a testdata case), and record the per-repository numbers, the natscli trial result and the review outcome in design.md
- [x] 6.4 Update `docs/design.md`: §3.5 names the migration family, this change and `natsvet migrate apply` as the next one; §6 records the change; §8 item 6 is closed; add a "Migrating off the legacy JetStream API" section to the README pointing at `natsvet migrate plan`, `natsvet migrate skill` and `natsvet-migrate.json`; verify the README rule table still matches `Analyzers()`
- [x] 6.5 Run `gofmt -l .`, `go vet ./...`, `staticcheck ./...`, `misspell -locale US .`, `make lint`, `make test` (with the testdata download step), `make corpus` and `make plugin`; verify all pass with no output from gofmt and misspell
